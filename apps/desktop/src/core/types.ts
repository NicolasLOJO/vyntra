export interface WidgetSummary {
  id: string;
  name: string;
  version: string;
  size_w: number;
  size_h: number;
}

export interface GridPosition {
  x: number;
  y: number;
  w: number;
  h: number;
}
