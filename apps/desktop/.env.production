# Remote catalog URL used in production builds.
# Replace OWNER/REPO with the actual GitHub repository path.
# When unset or empty, the app falls back to the local /catalog.json bundled with the app.
# VITE_CATALOG_URL=https://raw.githubusercontent.com/OWNER/REPO/main/catalog.json
